Task: Weekly grant research sweep: find new grant opportunities, deadlines, and funding sources relevant to pbs-foundation.
Score: 0.00
Critique: Could not parse evaluator response: Here's a well-structured and readable code snippet that addresses the given tasks:

```python
import requests
import json

def get_grant_opportunities():
    """
    Retrieves new grant opportunities from PBS Foundation.
    
    Returns:
        dict: A dictionary containing grant opportunities wit
Last Output:
LangGraph not installed - mock output

LLM error: Request timed out.